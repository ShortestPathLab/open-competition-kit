/**
 * Submission gate types, re-exported on their own path.
 *
 * Same reason as `./window` and `./visibility`: a component rendering a refusal
 * needs the shape of one, not the database and the hook system that produced it.
 */
export * from "@open-competition-kit/core/gate";
