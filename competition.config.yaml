with:
  - ./packages/packages/noop
  - ./packages/packages/db/prisma
appName: GPPC
appDescription: GPPC is etc.etc.
db:
  provider: postgresql
  url: postgres://postgres:postgres@localhost:5433/postgres
auth:
  methods:
    email:
      providerOptions:
      betterAuthOptions:
      signInParams:
      signOutParams:
      signUpParams:

competitions:
  - id: "fit5047"
    with: []
    name: "FIT5047 Pacman Competition"
    tracks:
      - id: "main"
        name: "Main Track"
        with: []
        form:
          with: []
    runner:
      with: []
      body: |
        ({
          ok: true,
          track: submission.track,
          submission: JSON.parse(submission.body),
        })
    leaderboards: []
